name: hanet_webrtc_player
description: A WebRTC video player widget component for Flutter applications.
version: 0.0.2
homepage: https://github.com/dieunt/hanet_webrtc_player
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # flutter_webrtc: 0.12.12+hotfix.1
  flutter_webrtc:
    git:
      url: https://github.com/dieunt/hanet_flutter_webrtc.git
      ref: 0.0.2
  flutter_blue: ^0.8.0
  flutter_mdns_plugin: ^0.0.3
  shared_preferences: ^2.0.9
  path_provider: ^2.0.7
  event_bus: ^2.0.0  
  uuid: ^4.4.0
  intl: ^0.19.0
  video_player: ^2.8.1
  web_socket_channel: ^3.0.2
  html: ^0.15.4
  flutter_image_gallery_saver: ^0.0.2
  image_gallery_saver: ^1.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0